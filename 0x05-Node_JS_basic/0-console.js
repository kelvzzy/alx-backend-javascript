/**
 * Displays a message on the STDOUT.
	* @param {Strings} msg The message to display.
	* @author Kelvin Uche <https://github.com/kelvzzy>
	*/
const displayMessage = (msg) => {console.log(msg);
};

module.exports =  displayMessage;
